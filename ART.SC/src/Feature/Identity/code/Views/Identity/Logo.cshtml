@using ART.SC.Feature.Identity
@using ART.SC.Feature.Identity.Repositories
@using ART.SC.Foundation.SitecoreExtensions.Extensions
@using Sitecore.Mvc
@model Sitecore.Mvc.Presentation.RenderingModel
@{

    var identity = IdentityRepository.Get(Model.Item);
    if (identity == null)
    {
        return;
    }
    //TODO: Resolve context site from item (implement in Foundation.Multisite)
    var homeUrl = "/";
}

    <ul class="menu">
        <li> <a href="@homeUrl">@Html.Sitecore().ImageField(ART.SC.Feature.Identity.Templates.Identity.Fields.Logo, identity, mh: 50)</a></li>

    </ul>